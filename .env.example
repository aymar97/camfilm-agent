# =====================================================
# EXEMPLE de configuration (sans vraies clés)
# Copie ce fichier vers .env et remplis tes vraies clés.
# .env n'est JAMAIS poussé sur GitHub (protégé par .gitignore).
# =====================================================

# --- Gemini (Google AI Studio) ---
GEMINI_API_KEY=ta_cle_gemini_ici
GEMINI_MODEL=gemini-2.5-flash

# --- Parallel Search API (SDK officiel) ---
PARALLEL_API_KEY=ta_cle_parallel_ici
PARALLEL_SEARCH_URL=https://api.parallel.ai/v1beta/search

# --- Google Cloud (ID du projet, non sensible) ---
GOOGLE_CLOUD_PROJECT=camfilm-agent-506923